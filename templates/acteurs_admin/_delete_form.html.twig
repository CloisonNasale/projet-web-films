<form method="post" action="{{ path('app_acteurs_admin_delete', {'id': acteur.idActeur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acteur.idActeur) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>

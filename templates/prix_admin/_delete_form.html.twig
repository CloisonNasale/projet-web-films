<form method="post" action="{{ path('app_prix_admin_delete', {'id': prix.idprix}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prix.idprix) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>

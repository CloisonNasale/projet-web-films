<form method="post" action="{{ path('app_pays_admin_delete', {'id': pay.idpays}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pay.idpays) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
